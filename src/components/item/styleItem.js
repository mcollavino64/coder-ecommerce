import styled from "styled-components";

export const StyleItemFont = styled.div`

h3 {
    font-size: clamp(1rem, 3vw, 2rem);
    color: #F7FF00 ;
}

p {
margin-bottom: 1rem;
    font-size: clamp(1rem, 3vw, 2rem);
}
`;